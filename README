This is Perl module AnySan::Provider::Lingr.

INSTALLATION

Type the following command:

    $ curl -L http://cpanmin.us | perl - AnySan::Provider::Lingr

Or install cpanm and then run the following command to install
AnySan::Provider::Lingr:

    $ cpanm AnySan::Provider::Lingr

If you get an archive of this distribution, unpack it and build it
as per the usual:

    $ tar xzf AnySan-Provider-Lingr-$version.tar.gz
    $ cd AnySan-Provider-Lingr-$version
    $ perl Makefile.PL
    $ make && make test

Then install it:

    $ make install

DOCUMENTATION

AnySan::Provider::Lingr documentation is available as in POD. So you can do:

    $ perldoc AnySan::Provider::Lingr

to read the documentation online with your favorite pager.

LICENSE AND COPYRIGHT

Copyright (c) 2012, <<YOUR NAME HERE>>. All rights reserved.

This library is free software; you can redistribute it and/or modify
it under the same terms as Perl itself.
